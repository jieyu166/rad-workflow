## 1. Canonical data and reviewed content

- [x] 1.1 Implement **Canonical schema and immutable chapter timing** in `skills/lecture-to-notes/scripts/lecture_model.py`: deliver **Canonical lecture schema and normalization** for aliases, legacy bullets/frames/OCR, safe relative paths, structured frame findings, UTF-8 atomic JSON writes, and **Chapter timing is immutable** across count/start/end values; verify RED then GREEN with `python -m unittest tests.test_lecture_model -v`, including absolute Windows/POSIX paths, malformed/non-finite times, missing frames, OCR type, time-change segment index, and no leftover temp file.
- [x] 1.2 Implement **Evidence-bound content rewrite and privacy gate** through deterministic content validation and evidence review in `lecture_content_rules.py` and `rewrite_evidence.py`: deliver **Focused Traditional Chinese content**, **Speaker content and editorial knowledge remain separated**, **Claim-level trusted evidence and human support attestation**, and the evidence-packet portion of **Evidence-bound and privacy-gated rewrite**. Treat each `summary_zh` and each of exactly four indexed `takeaways_zh`, including unchanged legacy clauses, as five fully covered speaker claims; treat every `title` as a sixth reviewed content unit with explicit string `title_kind` equal to exactly `topical` or `case_claim`. Require `topical` titles to have an empty title citation set, no title claim attestation, and digest-bound reviewer `topical_title_confirmed=true`; require `case_claim` titles to have trusted citations and per-claim `support_confirmed=true` attestation. Human review owns semantic classification; mechanical pathology/follow-up/history/case red flags can force `case_claim` but their absence must never prove `topical`. Implement a non-circular two-digest review contract: `approved_candidate_sha256` covers chapter identity/times, title, `title_kind`, summary, exactly four ordered takeaways, editorial notes, and an order-insensitive canonical citation map; `review_attestation_sha256` separately covers candidate digest, packet SHA-256, canonical per-claim records (normalized claim SHA-256, canonical citation identity/hash set, `support_confirmed=true`), Unicode-NFC/trimmed non-empty reviewer identity, `source_support_confirmed=true`, `case_facts_confirmed=true`, `editorial_separation_confirmed=true`, boolean `topical_title_confirmed`, and `review_schema="lecture-content-review"` plus integer `review_version=1` without containing itself. Approved run state must bind and verify both digests. The validator must check content-unit/claim coverage, title classification and its confirmation/evidence contract, citation type/existence/trust, external frame manifest, both digests, reviewer/confirmation/schema and bindings without claiming regex, exact, or fuzzy matching proves semantic classification/support; fail closed on missing/non-string/out-of-enum `title_kind`, topical evidence/confirmation mismatch, case-claim evidence gaps, title classification tamper/replay, missing or tampered evidence, reviewer/attestation mutation, either approved-run digest mismatch, or replay after any title, summary, takeaway, editorial-note, chapter identity/time, or citation membership/type/hash change; accept citation reordering alone after canonicalization; keep `editorial_notes_zh` separate and unusable as case support; preserve existing content rules and sensitive-pattern scan; and emit findings without claim/source text. Verify RED/GREEN with `python -m unittest tests.test_lecture_content_rules -v`, covering generic `後顱窩腫瘤影像判讀策略` topical PASS, topical missing-confirmation/evidence mismatch FAIL, supported `case_claim` PASS, implicit pathology `左側小腦轉移瘤`, follow-up `術後追蹤無復發`, and history `肺癌病史併新發腦病灶` requiring case classification/citations, missing/wrong-type/out-of-enum/classification-tampered or replayed title FAIL, supported paraphrase PASS, externally manifested frame PASS, unchanged uncited legacy FAIL, unrelated citation/binding mismatch FAIL, missing clause coverage FAIL, packet-only forged frame FAIL, editorial separation, full-candidate replay for title/`title_kind`/editorial/summary/takeaway/citation membership/type/hash changes, citation reorder PASS, reviewer swap with unchanged candidate FAIL, confirmation/schema/attestation tamper FAIL, and missing either approved-run digest FAIL.

## 2. Frame staging and rewrite application

- [ ] 2.1 Implement **Staging-only candidate frames and deterministic 1–4 frame curation** in `slide_frames.py`, `ocr_frames.py`, and `frame_curator.py`: deliver **Candidate frames exist only in staging**, **Supported deterministic scene extraction**, **Legacy formal frames are materialized safely**, **Each chapter has one to four formal frames**, and **Frame paths and boundary tolerance are auditable**; pin/record `scenedetect>=0.6.4,<0.8`, copy and SHA-256 verify all 72 first-lecture legacy files, deduplicate path/time candidates, reject zero valid frames, and never mutate canonical JSON during extract/OCR; verify with `python -m unittest tests.test_lecture_frame_curator -v` and `python -m py_compile` for the three scripts.
- [ ] 2.2 Implement the complete manual-first rewrite CLI in `rewrite_lecture.py`: finish **Evidence-bound and privacy-gated rewrite** by preserving times and formal frames, requiring packet-matched human review before apply, blocking external calls unless `--allow-external-llm`, `TEXT-EVIDENCE-ONLY`, and full-payload sensitive scan all pass, resolving credentials through zero-argument official SDK construction, checking stop reason, and treating external output only as a review candidate; verify without network access using `python -m unittest tests.test_lecture_rewrite -v` and `python -m py_compile skills/lecture-to-notes/scripts/rewrite_lecture.py`.

## 3. Single-source derived viewer and outputs

- [ ] 3.1 Implement **Single-flow derived renderers and typed search** plus **Responsive viewer and browser-observable interaction contract** in `build_lecture_viewer.py`: deliver **Viewer preserves core navigation and playback**, **Chapter presentation separates formal content**, **Formal frame layout is responsive and interactive**, **Search uses one typed semantic schema**, and **Embedded JSON is raw, parseable, and script-close safe** through the exact one-load/one-parse/five-argument render flow, one `build_blocks` index, formal frames only, modal/seek, stable hashes, `?t=`, synchronized transcript, 2-column above 768px and 1-column at or below 768px; verify with `python -m unittest tests.test_lecture_renderers.LectureRendererTests -v` and Python compile.
- [ ] 3.2 Implement JSON-only `.v4.md`, PBF, and course-hub rendering in `render_v4_note.py`, `json_to_pbf.py`, and `build_course_hub.py`: deliver **Derived outputs use canonical JSON as the sole content source**, exact chapter/content consistency, 11-lecture complete-status gate, and hub search objects that reuse `kind/label/start/end/text` with only `viewer/lecture_id/lecture_number` metadata; verify with `python -m unittest tests.test_lecture_renderers -v` and compile all three renderers.

## 4. Structured audit, preflight, and orchestration

- [ ] 4.1 Implement structured cross-derived audit in `lecture_audit.py` and the thin `check_lecture.py` facade: deliver **Audit is structured and fail-stop**, including lecture/stage/findings/input hashes/browser result, simultaneous collection of schema/content/viewer/PBF/V4 errors, raw viewer snapshot parsing, warning-only 1–3 frame handling, and non-zero status for every blocking finding; verify normal and cp950-strict runs of `python -m unittest tests.test_lecture_audit -v`.
- [ ] 4.2 Implement read-only-by-default preflight, installable PySceneDetect range, and strict skill-tree drift checks in `rebuild_course.py`, `requirements-rebuild.txt`, and `sync_skills.py`: deliver **Preflight blocks unsafe course processing** and **Live replacement probe is explicit and disposable** for exact formal pair inventory, writable roots, 3-vs-11 expected count, first-lecture 72 frames, ffmpeg/ffprobe/OCR/OpenCC/browser/dependency/space/sync gates, derivative JSON exclusion, and a uniquely named cross-root create/read/hash/replace/cleanup probe only when opted in; verify `python -m unittest tests.test_lecture_rebuild_pipeline.RebuildPreflightTests tests.test_sync_skills -v`, installed-version assertion, and `python sync_skills.py --check`.
- [ ] 4.3 Implement **Fail-stop audit and cp950-safe staged orchestration** in `rebuild_course.py`, `frame_curator.py`, and `batch_course.py`: deliver **Rebuild is staged, resumable, and failure-isolated** and **Progress and command output are cp950-safe** for ordered extract/curate/evidence/rewrite-apply/render/audit stages, per-chapter events, exact `--only`, full-content MP4/SRT/JSON hash resume protection, failure isolation during generation, and a compatibility wrapper that returns the real exit code; verify with cp950 strict `python -m unittest tests.test_lecture_rebuild_pipeline tests.test_lecture_console_encoding -v`.

## 5. Transactional publication

- [ ] 5.1 Implement **Manifested same-filesystem publication and whole-lecture rollback** in `publish_transaction.py`: deliver **Backup and run identities use sortable local timestamps**, **Each lecture publishes as one manifested transaction**, **Any lecture replacement failure triggers whole-lecture rollback**, and **Recovery evidence survives manifest persistence failure** by resolving local `YYYYMMDD-HHMMSS` run IDs before state creation, requiring the identical ID in backup directory/name, deterministically selecting the lowest available `-01`, `-02`, and subsequent decimal collision suffix, rejecting arbitrary non-temporal IDs before backup/live mutation, recording the resolved run ID and backup path in manifest/recovery evidence, and preserving named manifest fields, immutable staging, verified backups, sibling temp plus `os.replace`, reverse rollback, deletion of transaction-created files, retry, incomplete-transaction recovery, and external recovery evidence; verify the timestamped-path, same-second collision, invalid-ID, traceability, and complete failure-injection matrix with `python -m unittest tests.test_lecture_publish_transaction -v`.

## 6. Synthetic integration and browser evidence

- [ ] 6.1 Build the non-sensitive deterministic fixture and injected pipeline adapter under `tests/fixtures/lecture_rebuild/`: deliver the fixture part of **TDD fixture and browser E2E cover boundary behavior**, using real tiny MP4/SRT/JSON/PNG assets, formal frame counts `[1,2,3,4]`, preserved empty OCR, invalid path/time/OCR fixture, and an isolated zero-frame lecture failure while production render/audit code remains in the path; verify `python -m unittest tests.test_lecture_rebuild_pipeline.FixturePipelineTests -v` and staged/source frame SHA-256 equality.
- [ ] 6.2 Implement the fixed-allowlist headless browser probe and E2E test in `build_lecture_viewer.py` and `tests/test_lecture_viewer_e2e.py`: complete **TDD fixture and browser E2E cover boundary behavior** by opening the actual temporary staging viewer, parsing both JSON scripts, loading/decoding all ten formal images, measuring 769px/768px columns/gap/overflow, exercising delegated modal clicks, seek/search/details/deep links, and proving actual synthetic MP4 time advancement plus transcript highlighting; verify a real Chrome or Edge PASS with `python -m unittest tests.test_lecture_viewer_e2e -v` rather than accepting a skip on the implementation machine.

## 7. Operator contract and controlled rollout gates

- [ ] 7.1 Finalize `SKILL.md` and the production CLI contract in `rebuild_course.py`: deliver **Behavior and interfaces**, **Scope boundaries**, and **Publication receipt is bound to roots, sources, and run** for two-phase build/review resume, exact sample/count controls, explicit `20150804-NR` confirmation, receipt binding to live/staging/backup roots, expected count, source full-content hashes, timestamp-derived run ID and `course-run.json` SHA-256; require `preflight.json`, `course-run.json`, lecture/homepage manifests, and external recovery evidence to carry the same resolved `YYYYMMDD-HHMMSS[-NN...]` identity and backup path, reject non-temporal or mismatched identities before manifest creation, stop immediately before an audit-failed or publish-failed lecture, and deliver **Homepage publishes last as an independent transaction** only after 11 committed manifests and passing course audit; verify CLI tests cover timestamped backup paths, deterministic same-second suffix allocation, resumed-state mismatch, and arbitrary-ID rejection, then run `python sync_skills.py`, `python sync_skills.py --check`, and confirm derived copies are not staged.
- [ ] 7.2 After separate permission for local/de-identified source access, execute the three-lecture Brain tumor/Vascular/Spine sampling gate outside Git: exercise **Representative and full-course gates precede publication** by selecting exactly one unique lecture per category, running preflight and foreground extract/curate/evidence, completing packet-matched manual reviews, then rewrite/render/audit and browser checks; verify one three-lecture `course-run.json`, three `ok=true` audits, no invented case details, and record only external evidence paths and hashes, never course content.

## 8. Full verification and NAS handoff

- [ ] 8.1 Execute **TDD fixture E2E sampling and fresh verification gates** and prove the design **Acceptance criteria**: run all `test_lecture_*.py`, cp950 strict suites, compile checks, `sync_skills.py --check`, `git diff --check`, status and secret/output scans, then dispatch a fresh-context verifier to map every spec requirement to file/symbol or command evidence; any failure receives a focused fix commit and the full gate repeats until no FAIL remains.
- [ ] 8.2 Preserve the exact 11-lecture operational runbook without performing it during apply: enforce **NAS operations and cleanup require separate authorization** and the full-course portion of **Representative and full-course gates precede publication**; after a new explicit NAS/long-run/publication authorization, freeze the verified Git revision, use same-share staging/backup, run opt-in probe-backed preflight, build/review/audit 11 lectures, publish one lecture at a time with immediate fail-stop, run post-publish smoke/E2E, publish homepage last, support complete lecture/homepage rollback, and retain evidence until separately authorized cleanup; verify each transition from preflight receipt, audit, manifest state and SHA-256 evidence, and perform no NAS action without that authorization.
